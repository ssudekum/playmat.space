/**
 * A representation of the Scryfall ImageURI object, describing various properties of that image.
 */
export default interface ImageURI {
    art_crop: string,
    border_crop: string,
    large: string,
    normal: string,
    png: string,
    small: string
}